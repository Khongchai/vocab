/**
 * Unfortunately, we can't expect that anyone will support this langauge out-of-the-box. We have to
 * piggy-back variable colors for now.
 */

export const tokenTypes = ["newVocab", "reviewedVocab"];
export const tokenModifiers = [];
